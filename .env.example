# Point this at your deployed FastAPI backend (e.g. Render).
# If unset, the app defaults to http://localhost:8000 and works fully
# standalone — the backend is optional (used only for the "Verify against
# backend" cross-check and for real login; guest mode always works locally).
VITE_API_BASE_URL=https://your-backend.onrender.com
